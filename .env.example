# UpRez Monorepo Configuration
# Copy this to .env for local development

# 1. AI & Logic
OPENAI_API_KEY=sk-your-key-here
NEXT_PUBLIC_USE_OPENAI=true

# 2. Database (Vercel Postgres / Neon)
# In local development, you can use a local Postgres or a Neon connection string
POSTGRES_URL=postgres://user:password@hostname:5432/dbname
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# 3. Email Configuration (SendGrid Recommended)
EMAIL_PROVIDER=sendgrid
SENDGRID_API_KEY=SG.your-key-here
SENDGRID_FROM_EMAIL=noreply@yourdomain.com
SENDGRID_FROM_NAME="UpRez Demo"

# Master Override: Where to send ALL demo emails
CONTACT_EMAIL=your-inbox@example.com

# 4. URLs & Networking
# Frontend/App URL (Used for absolute links in emails)
NEXT_PUBLIC_FRONTEND_URL=https://up-rez.vercel.app

# Backend URL (Monorepo handles this via /api, but kept for legacy client logic if needed)
NEXT_PUBLIC_BACKEND_URL=/api

# 5. Asset Configuration
# If hosting images on a different CDN/Host
NEXT_PUBLIC_IMAGE_BASE_URL=https://up-rez.vercel.app
